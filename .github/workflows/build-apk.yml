name: Build Android APK

# Builds a debug-signed APK that can be sideloaded onto a phone for UI / lite-mode
# preview. The APK is uploaded as a workflow artifact — download it from the
# Actions tab on github.com/calinnedelcu/myjarvis/actions.
#
# Triggers:
#   - push to master (any change under mobile/ or this workflow file)
#   - manual run (Actions tab → Build Android APK → Run workflow)

on:
  push:
    branches: [master]
    paths:
      - "mobile/**"
      - ".github/workflows/build-apk.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: "3.24.5"
          cache: true

      - name: Generate Android platform folder
        working-directory: mobile
        run: |
          flutter create . \
            --project-name jarvis_mobile \
            --org com.myjarvis \
            --platforms=android
          ls -la android/

      - name: Patch AndroidManifest with required permissions
        working-directory: mobile
        run: |
          MANIFEST=android/app/src/main/AndroidManifest.xml
          python3 - <<'PY'
          import re, pathlib
          p = pathlib.Path("android/app/src/main/AndroidManifest.xml")
          src = p.read_text()

          extra_perms = """
              <uses-permission android:name="android.permission.INTERNET"/>
              <uses-permission android:name="android.permission.RECORD_AUDIO"/>
              <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
              <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
              <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
              <uses-permission android:name="android.permission.WAKE_LOCK"/>
              <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
          """

          # Insert permissions just before <application> if not already present.
          if "RECORD_AUDIO" not in src:
              src = re.sub(
                  r"(<manifest[^>]*>)",
                  r"\1" + extra_perms,
                  src,
                  count=1,
              )

          # Allow plain HTTP traffic (Tailscale endpoints have no TLS).
          if "usesCleartextTraffic" not in src:
              src = re.sub(
                  r"(<application\b)",
                  r'\1 android:usesCleartextTraffic="true"',
                  src,
                  count=1,
              )

          p.write_text(src)
          print(src)
          PY

      - name: Bump Gradle / Android SDK if needed
        working-directory: mobile
        run: |
          # flutter_local_notifications 17.x requires compileSdk >= 34
          BUILD_GRADLE=android/app/build.gradle
          if [ -f "$BUILD_GRADLE" ]; then
            sed -i 's/compileSdkVersion .*/compileSdk 34/' "$BUILD_GRADLE"
            sed -i 's/targetSdkVersion .*/targetSdkVersion 34/' "$BUILD_GRADLE"
            sed -i 's/minSdkVersion .*/minSdkVersion 23/' "$BUILD_GRADLE"
            cat "$BUILD_GRADLE"
          fi

      - name: Flutter pub get
        working-directory: mobile
        run: flutter pub get

      - name: Build debug APK
        working-directory: mobile
        run: flutter build apk --debug --target-platform android-arm64

      - name: Rename APK with run number
        working-directory: mobile
        run: |
          mkdir -p out
          cp build/app/outputs/flutter-apk/app-debug.apk \
             out/jarvis-mobile-${{ github.run_number }}.apk
          ls -la out/

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: jarvis-mobile-apk
          path: mobile/out/*.apk
          retention-days: 14
          if-no-files-found: error
